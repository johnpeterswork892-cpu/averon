import { Inter } from "next/font/google";
import type { Metadata } from "next";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Averon Express - Courier & Logistics Services",
  description:
    "Fast, reliable, and global courier services for all your shipping needs",
  icons: {
    icon: '/assets/Averon_Express_Logo_Transparent.png',
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
